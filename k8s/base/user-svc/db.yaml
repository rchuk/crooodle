apiVersion: v1
kind: Service
metadata:
  name: user-svc-db
  labels:
    app: user-svc-db
spec:
  ports:
    - name: tcp
      port: 5432
      targetPort: 5432
  clusterIP: None
  selector:
    app: user-svc-db
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: user-svc-db
spec:
  serviceName: user-svc-db
  replicas: 1
  selector:
    matchLabels:
      app: user-svc-db
  template:
    metadata:
      labels:
        app: user-svc-db
    spec:
      containers:
        - name: postgres
          image: postgres:18.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: user-svc-db-secret
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          readinessProbe:
            exec:
              command: ["bash", "-c", "pg_isready -U $POSTGRES_USER -d $POSTGRES_DB"]
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: 5432
            initialDelaySeconds: 20
            periodSeconds: 10
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: user-svc-db-pvc
