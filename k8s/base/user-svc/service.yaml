apiVersion: v1
kind: Service
metadata:
  name: user-svc
  labels:
    app.kubernetes.io/name: user-svc
spec:
  selector:
    app.kubernetes.io/name: user-svc
  ports:
    - name: http
      port: 80
      targetPort: 8080
  type: ClusterIP
