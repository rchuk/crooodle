<project>
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.ukma.spring.crooodle</groupId>
		<artifactId>svc-parent</artifactId>
		<version>1.0.0-SNAPSHOT</version>
		<relativePath>../parents/svc-parent/pom.xml</relativePath>
	</parent>

	<artifactId>api-gateway</artifactId>

	<properties>
		<svc.mainClass>org.ukma.spring.crooodle.gateway.ApiGatewayApplication</svc.mainClass>
	</properties>

	<dependencies>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-gateway</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.cloud</groupId>
			<artifactId>spring-cloud-starter-netflix-eureka-client</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-actuator</artifactId>
		</dependency>
	</dependencies>

	<build>
		<finalName>${project.artifactId}</finalName>
	</build>
</project>
