package org.ukma.spring.crooodle.service;

import org.springframework.stereotype.Service;
/*
import org.ukma.spring.starter.WelcomeService;

@Service
public class MessageService {

    private final WelcomeService myCustomService;

    // Інжектимо сервіс через конструктор
    public MessageService(WelcomeService myCustomService) {
        this.myCustomService = myCustomService;
    }

    // Метод, що використовує логіку сервісу для отримання повідомлення
    public String createWelcomeMessage(String name) {
        return myCustomService.formatMessage(name);
    }
}
*/
